app:
  build:
    base: [ nodejs@14 ]
    build:

      - ls -a
      - yarn
      - sudo cp -fr ./env.js ./node_modules/react-scripts/config/env.js
      - yarn build
    deploy: [ 'build/~/' ]
  run:
    envReplace:
      target: [ '.' ]
      delimiter: [ '%%' ]
