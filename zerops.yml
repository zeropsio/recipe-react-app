app:
  build:
    base: [ nodejs@14 ]
    build:
      - echo "$REACT_APP_RECIPE_CONFIG"
      - yarn
      - yarn build --prod
    deploy: [ 'build/~/' ]
  run:
    envReplace:
      target: [ '.' ]
      delimiter: [ '%%' ]
