app:
  build:
    base: [ nodejs@14 ]
    build:
      - yarn
      - yarn build --prod && echo "$REACT_APP_RECIPE_CONFIG"
    deploy: [ 'build/~/' ]
  run:
    envReplace:
      target: [ '.' ]
      delimiter: [ '%%' ]
