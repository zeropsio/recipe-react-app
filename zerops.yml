app:
  build:
    base: [ nodejs@14 ]
    build:

      - ls -a
      - sudo cp -fr ./env.js ./node_modules/react-scripts/config/env.js
      - yarn
      - yarn build
    deploy: [ 'build/~/' ]
  run:
    envReplace:
      target: [ '.' ]
      delimiter: [ '%%' ]
